# Plan: [Feature Name]

> Copy this template to `plans/active/YYYY-MM-DD-short-slug.md` before starting work (or run `/new-plan`).
>
> When shipping: (1) update or create `docs/systems/{relevant}.md`, (2) move this file to `plans/shipped/`, (3) remove the item from `docs/sprint-backlog.md` and `docs/roadmap.md`. If cancelled, move to `plans/cancelled/`.

## Status
- [ ] Drafting
- [ ] Approved to start
- [ ] In progress
- [ ] Shipped
- [ ] Cancelled

## Problem
One to three sentences. What are we solving? No framing, no backstory. Just the problem.

## Acceptance criteria
Specific, testable, each one observable from the outside.

- [ ] A user can do X and see Y
- [ ] Endpoint Z returns 200 on valid input, 400 on invalid
- [ ] Smoke test passes post-deploy

## Changes by area

<!-- Adapt sections to your project. Common ones:

### Schema changes
- Migration file name, columns, indexes. Or "none."

### Server changes
- Endpoints, RPCs, engine functions affected.

### Client changes
- Components, stores, API calls affected.

### Data changes
- Files to add or modify.

### Test plan
- Unit tests to add. Integration check. Manual smoke path.
-->

## Rollback plan
If this ships broken, how do we undo it without data loss?

## Deferred (intentional cuts)
Things considered but out of scope.

## System doc impact
Which `docs/systems/*.md` file(s) need updating when this ships? If none exists for this area, a new one will be created.

## Links
- Relevant files:
- Relevant plans:
- Sprint backlog item:

---

## Session log

### Session 1 — [Date]
**Goal:** ...

**Shipped:**
- ...

**Didn't ship / blocked:**
- ...

**Tech debt introduced:**
- ...

**Next session's first move:**
- ...
